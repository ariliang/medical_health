# medical_health

## 数据结构

```python
# 所有问题
results = [
    # 某个问题
    [
        # 问题或答案的描述；是问题还是答案
        {'desc': 'q1', 'is_que': 1},
        {'desc': 'a1', 'is_que': 0}
    ],
    # 某个问题
    [
        {'desc': 'q2', 'is_que': 1},
        {'desc': 'a2', 'is_que': 0}
    ]
]
```